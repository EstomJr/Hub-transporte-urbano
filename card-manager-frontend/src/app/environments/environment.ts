export const environment = {
  production: false,
  authApiUrl: 'http://localhost:8080/auth', // Ajuste a porta conforme seu Gateway ou Auth Service
  userApiUrl: 'http://localhost:8081/users' // Ajuste a porta conforme seu Gateway ou User Service
};
